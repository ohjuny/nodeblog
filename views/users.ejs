<%- include('header') -%>

<h1>Users</h1>
<ul>
  <%
    var list = '';
    for (i = 0; i < users.length; i++) {
      list += '<li>' + users[i].username + ': ' + users[i].email;
      list += '   <button method="post" action="/users/delete">X</button>'
      list += '</li>';
    }
  %>
  <%- list %>
</ul>

<%- include('footer') -%>